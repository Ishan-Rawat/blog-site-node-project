<!DOCTYPE html>
<html lang="en">
<!-- Notice that instead of using <"%=" we used <"%-" -->
<!-- This is because <"%=" coverts the content into a string -->
<%- include('./partials/head.ejs') %>

<body>
    
    <%- include('./partials/nav.ejs') %>

    <div class="blogs content">
        <h2>All Blogs</h2>
        <!-- what we want to do now is make the individual blogs act like links to their "own" page -->
        <!-- So will surround the h3 and p tags with an anchor tag -->
        <% if(blogs.length > 0) { %>
            <% blogs.forEach(blog => { %>
                <a class="single" href="/blogs/<%= blog._id %>">
                    <!-- What the above code does is that upon clicking a blog it takes us to a URL such as: -->
                    <!-- http://localhost:3000/blogs/647c6df6a5e6546b217b193d -->
                    <h3 class="title"><%= blog.title %></h3>
                    <p class="snippet"><%= blog.snippet%></p>
                </a>
            <% }) %>
        <% } else {%>
            <p>No logs to display at the moment (；′⌒`)</p>
        <% } %>
    </div>
    <%- include('./partials/footer.ejs') %>
</body>
</html>